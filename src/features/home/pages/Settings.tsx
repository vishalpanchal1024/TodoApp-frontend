import { FC, ReactElement } from "react"

const Settings:FC = ():ReactElement => {
  return (
    <div>
      <h2 className="text-3xl md:text-5xl" >Settings</h2>
    </div>
  );
};

export default Settings;
