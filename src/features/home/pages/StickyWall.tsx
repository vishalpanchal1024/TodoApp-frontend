import { FC, ReactElement } from "react";

const StickyWall:FC = ():ReactElement => {
  return (
    <div>
      <h2 className="text-3xl md:text-5xl" >Sticky Wall</h2>
    </div>
  );
};

export default StickyWall;
